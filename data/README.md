# Tutorial data

Phase 1 deliberately contains no biological fixtures. Every future source snapshot
and transformed GKS output must be declared in `manifest.yaml`, stored separately,
and documented with source, selection, license, retrieval, transformation, model
versions, and SHA-256 checksums.
