# Literature source snapshot

`PMID23220880-esummary.json` is the unmodified PubMed ESummary response for
PMID `23220880`, retrieved on 2026-07-22. The paper reports HER2 V777L as an
activating mutation; the discovery notebook uses that source-native protein
label as its starting point.

The bibliographic record does not itself provide a VRS identifier. The
notebook's link from the paper label to a Cat-VRS concept is explicitly a
tutorial-curated candidate mapping, not an assertion copied from PubMed or the
paper.

Maintainers can refresh the snapshot with:

```bash
uv run python scripts/refresh_pubmed_native_fixture.py
```
