# Tutorial authoring

Use the standard sections specified in the implementation plan, relative paths,
small outputs, no secrets or hidden state, and a no-op-local Colab bootstrap when
Colab support is added. Pair `.ipynb` and percent-format `.py` files with Jupytext.
Application logic belongs in the package and requires unit tests.
